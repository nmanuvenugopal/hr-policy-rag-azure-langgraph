# ============================================================
# HR RAG pipeline - pinned environment (Python 3.13, macOS)
# Regenerate a full freeze anytime with:  pip freeze > requirements-lock.txt
# ============================================================

# ---- LangChain ecosystem (versions from your working venv) ----
langchain==1.3.16
langchain-core==1.6.0
langchain-community==0.4.1     # PINNED <0.4.2: ragas 0.4.3 imports the removed
                               # langchain_community.chat_models.vertexai path
                               # (ragas GitHub issue #2745). Unpin when ragas fixes it.
langchain-openai==1.1.9
langchain-text-splitters==1.1.2
langchain-experimental==0.4.2  # declares community>=0.4.2 but verified working on 0.4.1
langgraph==1.2.11
langsmith==0.11.1

# ---- Evaluation ----
ragas==0.4.3                   # keep in lockstep with the community pin above

# ---- Azure SDKs ----
# Run `pip freeze | grep -i azure` and replace with your exact versions:
azure-ai-documentintelligence>=1.0.0
azure-search-documents>=11.5.0
azure-core>=1.30.0

# ---- Models / APIs ----
openai==1.109.1
cohere>=5.0                    # optional now (replaced by Azure semantic ranker);
                               # keep if you want the fallback path, else delete

# ---- Observability ----
logfire>=3.0                   # run `pip freeze | grep logfire` for the exact pin

# ---- Data / core ----
pydantic==2.13.4
pandas==3.0.5
numpy==2.5.2
datasets==5.0.1

# ---- Notebook ----
ipykernel